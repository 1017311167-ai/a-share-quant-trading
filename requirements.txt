# ===== A股量化回测软件 依赖清单 =====
# 安装方式：pip install -r requirements.txt

# ---------- Web 图形界面 ----------
streamlit>=1.60

# ---------- 向量化回测引擎（速度快） ----------
vectorbt>=1.0

# ---------- A股免费数据源（无需注册） ----------
akshare>=1.18

# ---------- 图表绘制（回测结果可视化） ----------
# 注意：vectorbt 1.x 与 plotly 7 不兼容（scattermapbox 被移除），需锁定 6.x
plotly>=6.0,<7.0

# ---------- 数值计算与数据处理 ----------
pandas>=3.0
numpy>=2.4
numba>=0.66

# ---------- 桌面 GUI 与高性能图表 ----------
PyQt6>=6.11
pyqtgraph>=0.14

# ---------- 参数寻优：遗传算法 + 数值计算 + 并行加速 ----------
deap>=1.4.4
scipy>=1.18
joblib>=1.5

# ---------- 批量回测结果导出 Excel ----------
openpyxl>=3.1

# ---------- 环境变量与密钥管理 ----------
python-dotenv>=1.2

# ---------- 消息推送（微信/钉钉机器人） ----------
requests>=2.32
